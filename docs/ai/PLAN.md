# PLAN

No active task. Task #11 (CRUD: Lakes) closed — its full plan is preserved in git history
(`683ea49`) and in `docs/ai/planning/task-11-findings.md`.

Run `/uexel:plan <issue-number>` to start the next task. Candidates open now: **#30**
(p1, safety-relevant — cleared HIGH/CRITICAL alerts render as active) and **#20** (p1 —
`/lakes/$lakeId` renders the list, not the detail page).
